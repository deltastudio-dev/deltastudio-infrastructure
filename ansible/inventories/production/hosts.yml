all:
  children:
    webservers:
      hosts:
        deltastudio-server:
          ansible_host: 165.232.87.70  # Replace with actual IP
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_rsa